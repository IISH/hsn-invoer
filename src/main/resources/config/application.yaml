application:
  version: "5.6.3"
  date: "25 januari 2022"

storage:
  path: "/data/hsn/"

spring:
  web:
    locale: nl_NL

  mvc:
    format:
      date: dd-MM-yyyy

  servlet:
    multipart:
      max-file-size: '50MB'
      max-request-size: '50MB'

  http:
    multipart:
      enabled: true
      max-file-size: '50MB'
      max-request-size: '50MB'

  session:
    store-type: 'jdbc'
