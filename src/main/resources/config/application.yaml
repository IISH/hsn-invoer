application:
  version: "5.5.0"
  date: "22 oktober 2020"

storage:
  path: "/data/hsn/"

spring:
  mvc:
    locale: nl_NL
    format:
      date: dd-MM-yyyy

  servlet:
    multipart:
      max-file-size: '50MB'
      max-request-size: '50MB'

  http:
    multipart:
      enabled: true
      max-file-size: '50MB'
      max-request-size: '50MB'
